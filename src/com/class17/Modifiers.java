package com.class17;

public class Modifiers {
//1. access modifiers: public, protected, default, private
//2. non access modifiers: static, final, abstract 

	/*public - access to all classes in the entire project
	 * protected - access to all classes in the same package
	 * default - access to all classes in the same package
	 * private - access within same class ONLY */
// 		SYNTAX FOR METHODS:
	
	/*accessModifier (<<optional)
	 * nonAccessModifier (<<optional)
	 * returnType (<<must have)
	 * methodName (<<must have)
	 * (parameterList) (<<optional)
	 *{
* 	methodBody; 
* }
*/
// SYNTAX FOR VARIABLES:
	/* accessModifier (<<optional)
	 * nonAccessModifier (<<optional)
	 * dataType	(<<must have)
	 * name		(<<must have)
	 */
}


